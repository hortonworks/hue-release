Git file directory:
1) Go to directory <path>/tutorials/tutorials_app/run

2) in file run.sh chage variables:
    a) RUN_DIR (full path to directory RUN)
	b) GIT_REPO (GIT repository path)
	
3) configure CRON to run run.sh file